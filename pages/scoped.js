import React from 'react';
import ButtonScoped from '../components/ButtonScoped.js';

export default () => (
    <div>
    No styles applied, though they have been injected into the head.
        <ButtonScoped>Button!</ButtonScoped>
    </div>
);
